<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<!-- mvc 설정과 관련된 여러 처리를 담당하는 설정파일(ex: db, servie... ) -->
	<!-- servletconfig - 컨트롤러, mvc-config - dao..? -->

	<!-- 프로퍼티 파일 로딩하기 -->
	<context:property-placeholder location="classpath:/database/jdbc.properties" />


	<!-- db관련된 설정 -->
	<!-- db관련 정보를 커넥션 풀 객체에 담는 설정 -->

	<!-- HikariConfig 빈 등록 -->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<property name="driverClassName" value="${jdbc.driverClassName}" />
		<property name="jdbcUrl" value="${jdbc.url}" />
		<property name="username" value="${jdbc.username}" />
		<property name="password" value="${jdbc.password}" />

		<property name="maximumPoolSize" value="30" />
	</bean>

	<!-- DataSource 등록, 히카리데이터소스는 히카리컨피그없으면 못만든다 이것이 의존성 -->
	<bean id="ds" class="com.zaxxer.hikari.HikariDataSource">
		<constructor-arg ref="hikariConfig" />
	</bean>
	
	<!--  마이바티스 주요객체 sessionFactory클래스 빈 등록,   -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<!--  기본(프리미티브) int같은거는 value쓰고 배열같은건 참조타입은 ref사용 -->
		<property name="dataSource" ref="ds"/>
		<property name="configLocation" value="classpath:/spring/mybatis-config.xml"/>
		<property name="mapperLocations" value="classpath:/mappers/**/*Mapper.xml"/>
	</bean>
	
	<!--  마이바티스를 쉽게 사용할 템플릿 클래스 빈 등록 -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
		<constructor-arg ref="sqlSessionFactory"/>
	</bean>
	
	<!--  mapper.xml파일을 빈객체로 등록하는 설정  -->
	<!--  이 경로에 있는 xml파일의 인터페이스를 본체로 두겠다. -->
	<mybatis-spring:scan base-package="com.spring.myapp.user.repository"/>
	
	<!--  트랜잭션 매니저 빈 등록 -->
	<bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="ds" />	
	</bean>
	
	
	
	<!--  컴포넌트 자동 스캔 명령 -->
	<context:component-scan base-package="com.spring.myapp"/>


</beans>
